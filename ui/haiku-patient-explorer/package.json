{
  "name": "haiku-patient-explorer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate-data": "python3 scripts/generate_demo_data.py",
    "dev": "npm run generate-data && vite",
    "build": "npm run generate-data && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "dependencies": {
    "plotly.js-dist-min": "^3.0.1"
  }
}
