{
  "name": "haiku-patient-explorer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate-data": "python3 scripts/generate_demo_data.py",
    "dev": "vite",
    "build": "npm run generate-data && vite build",
    "build:pages": "GITHUB_PAGES=true npm run generate-data && GITHUB_PAGES=true vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "dependencies": {
    "plotly.js-dist-min": "^3.0.1"
  }
}
