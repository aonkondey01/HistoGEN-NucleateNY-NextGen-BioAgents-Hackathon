# Download + inspect the PHOENIX TCGA atlas (.h5ad / AnnData).
huggingface_hub[hf_xet]>=0.24   # Xet-accelerated transfer for large files
h5py>=3.10                       # inspect .h5ad without loading it all
# Optional, for working with the AnnData object directly:
# anndata>=0.10
# scanpy>=1.10
